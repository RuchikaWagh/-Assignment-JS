// 3. If let y=10 then find:
    // z=(++y * (y++ +5));

// Ans-  
         let y=10;
         z=(++y * (y++ +5));
         console.log(z)

    // => output - is 176 in number form.
//--------------------------------------------------------------------------*